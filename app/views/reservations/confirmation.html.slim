| #{@schedule.start.strftime("%Y年%m月%d日%H時")} ~ #{@schedule.close.strftime("%Y年%m月%d日%H時")}
  で予約を確定しますか？
= link_to "はい", reservations_path(schedule_id: @schedule.id, user_id: current_user.id), method: :post
